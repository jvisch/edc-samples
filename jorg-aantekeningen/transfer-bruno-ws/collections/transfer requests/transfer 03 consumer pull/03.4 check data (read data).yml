info:
  name: 03.4 check data (read data)
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:19291/public/
  headers:
    - name: Authorization
      value: "{{edr-authorization-attribute}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
