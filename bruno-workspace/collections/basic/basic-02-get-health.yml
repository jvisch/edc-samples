info:
  name: basic-02-get-health
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8181/api/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
